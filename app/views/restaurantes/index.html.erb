<h1>Listagem de Restaurantes</h1>
<table>
  <tr>
    <th>Nome</th>
    <th>Endereço</th>
    <th>Especialidade</th>
  </tr>
  <% @restaurantes.each do |restaurante| %>
    <tr>
      <td><%= restaurante.nome %></td>
      <td><%= restaurante.endereco %></td>
      <td><%= restaurante.especialidade %></td>
      <td><%= link_to 'Mostrar', restaurante_path(restaurante.id) %></td>
      <td><%= link_to 'Mostrar por nome', por_nome_restaurantes_path(restaurante.nome) %></td>
      <td><%= link_to 'Editar', edit_restaurante_path(restaurante.id) %></td>
      <td><%= link_to 'Abrir', abrir_restaurante_path(restaurante.id) %></td>
      <td><%= link_to 'Fechar', fechar_restaurante_path(restaurante.id) %></td>
    </tr>
  <% end %>
</table>
<br/>
<%=  %>
<%= link_to 'Novo', {action: 'new'} %>
